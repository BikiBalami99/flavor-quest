//This interface is used for inidivual Restaurant objects.
export interface RestaurantInterface {
  imageSrc: string;
  name: string;
  address: string;
  city: string;
  state: string;
  zipCode: string;
  category: string;
  rating: number;
  reviewCount: number;
}
